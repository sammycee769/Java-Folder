package Estore;

public class Admin extends Users{
    public Admin(int age, String name, String emailAddress, String homeAddress, String password, String phoneNumber) {
        super(age, name, emailAddress, homeAddress, password, phoneNumber);
    }
}
